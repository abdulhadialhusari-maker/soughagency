# تقرير إصلاح العناوين العربية — المراجعة النهائية V2

التاريخ: 2026-07-18 · الحالة: جاهز للمراجعة البشرية محليًا · النشر: لم يُنفذ

## التشخيص

أكد تسجيل الشاشة عند `1338×628` أن المشكلة بصرية فعلية: التفاف العناوين داخل كتلة نص واحدة، مع حجم كبير وتتبع سالب، جعل نقاط Alexandria وتشكيلها وامتداداتها تبدو متلامسة رغم أن صناديق الأسطر المحسوبة لم تكن متداخلة. لذلك لم يكن رفع `line-height` داخل الكتلة وحده كافيًا.

القياس السابق عند 1338px كان `62.217px / 80.882px` مع `letter-spacing: -2.80px` و`text-wrap: balance` والتفاف تلقائي.

## الإصلاح النظامي

- قُسمت العناوين الأربعة المعتمدة إلى سطرين صريحين داخل `.display-title__line`، مع الحفاظ على النص نفسه واسم وصول كامل عبر `aria-label`.
- أصبحت `.display-title` حاوية flex عمودية ذات فراغ فيزيائي مستقل: `14px` عند 320–375، `18px` في العروض المتوسطة، و`19.39px` عند 1338.
- حجم العرض أصبح `clamp(2rem, 4.05vw, 3.75rem)`؛ عند 1338 أصبح `54.189px`، أي أقل بنحو 13% من الحالة المرفوضة.
- `line-height: 1.12` لكل سطر، `letter-spacing: normal`، `overflow: visible`، ولا يوجد `text-wrap: balance` أو margin/transform سالب.
- عناوين البطاقات بقيت بتغليف تلقائي، مع clamp مستقل وتتبع طبيعي و`line-height: 1.50` (`1.52` على الشاشات الصغيرة) وهوامش واضحة قبل الوصف.
- لم تتغير الهوية، الخط، الألوان، النصوص، بنية الأقسام، بيانات التواصل، أو تسلسل الموشن وتوقيته.

## قياس مرجعي عند 1338×628

| العنصر | النتيجة |
| --- | --- |
| عناوين العرض | `54.189px / 60.692px`، نسبة `1.12` |
| الفراغ الفيزيائي بين السطرين | `19.39px` |
| تتبع الحروف | `normal` |
| عناوين البطاقات | `34.119px / 51.179px`، نسبة `1.50` |
| الحالة النهائية | `opacity: 1` و`transform: none` |

## نتائج QA

- اختُبرت المقاسات الدقيقة: `320×700`، `375×812`، `430×932`، `480×900`، `560×800`، `640×900`، `768×900`، `900×900`، `1024×768`، `1280×800`، `1338×628`، `1440×900`.
- صفر تمرير أفقي، صفر أخطاء Console، والخط Alexandria محمّل في جميع الاختبارات.
- CLS يساوي صفرًا في كل المقاسات عدا `0.00012` عند 900px، وهو أقل كثيرًا من حد الفشل `0.01`.
- فُحصت اللقطات كاملة الدقة بصريًا للعناوين والبطاقتين، والصفحات الكاملة عند 375 و560 و768 و1280 و1338 و1440: لا تلامس ولا قص للحروف أو النقاط أو التشكيل.
- هندسة عنوان الذكاء ثابتة قبل/أثناء/بعد الموشن: الارتفاع `140.766px` والفراغ `19.39px`. أثناء الحركة يتغير العرض البصري فقط، ثم يستقر إلى `transform: none`.
- `prefers-reduced-motion` يعرض الهندسة نفسها في الحالة النهائية فورًا.
- `npm run build` ناجح: TypeScript وVite؛ `163.19kB` JavaScript و`21.62kB` CSS قبل الضغط.
- لم يُنفذ أي نشر.

## الأدلة

- Before من تسجيل المستخدم: `qa-previews/typography-v2/before/`.
- After كاملة الدقة للعناوين والبطاقتين: `qa-previews/typography-v2/after/after-*-1338x628.png`.
- صفحات كاملة: `qa-previews/typography-v2/after/full-{375x812,560x800,768x900,1280x800,1338x628,1440x900}.png`.
- القياسات الخام: `qa-previews/typography-v2/after/typography-metrics.json`.
- موشن قبل/أثناء/بعد وتقليل الحركة: `qa-previews/motion/`.
