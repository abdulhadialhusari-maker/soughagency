# SOGH Public Website — Release Notes

## v2.3.1 — فحص إصدار الشريط

التاريخ: 2026-09-10
النطاق: تحقق وتلميع — **بلا نشر**، وبلا تبعيات جديدة.

الشريط كان منفذًا في v2.3. هذه الجولة تحقق منه بدل الاكتفاء بالنظر إليه.

### `npm run qa:marquee` — بوابة جديدة

تشغّل المقاسات الخمسة **مرتين**: مرة بالحركة ومرة مع فرض
`prefers-reduced-motion: reduce`، وتتحقق من كل قاعدة صراحةً:

- ستة شعارات معلنة مرة واحدة، بـ`aria-label` ونصوص بديلة كاملة.
- كل نسخة مكررة `aria-hidden="true"` وبـ`alt=""`.
- بلا روابط ولا `cursor: pointer` ولا نص مرئي.
- **النِّسب الأصلية:** يقارن كل شعار بأبعاده الفطرية ويرفض أي انحراف يتجاوز
  2٪ — يلتقط أي تشويه لا يمنعه `object-fit: contain` وحده.
- كل شعار داخل خانته، وبلا ظل وبلا حد على الخانة.
- التلاشي عند الحافتين موجود (يتحقق من عدد نقاط التدرج، فتلاشٍ على جهة واحدة
  يترك الحافة الأخرى مقصوصة).
- تحت تقليل الحركة: الحركة متوقفة، المكرر خارج العرض، والستة ظاهرة.
- صفر تمرير أفقي، صفر خطأ كونسول، وارتفاعات الأسطر داخل 1.22–1.32.

### النتيجة

عشر حالات (خمسة مقاسات × وضعين): **كلها OK**.

| | الحركة | تقليل الحركة |
| --- | --- | --- |
| 1440 / 1024 / 768 | 36s، تلاشٍ على الجهتين | ساكن، ستة شعارات |
| 390 / 360 | 45s، تلاشٍ على الجهتين | ساكن، ستة شعارات |

التمرير الأفقي صفر في كل حالة، والكونسول نظيف.

---

## v2.3 — شريط شعارات العملاء المتحرك

التاريخ: 2026-09-10
النطاق: قسم العملاء فقط — **بلا نشر**، وبلا تبعيات جديدة.

استُبدلت شبكة 3×2 بشريط أفقي متصل، بلا حدود خلايا وبلا معالجة تشبه البطاقات.

| | الكمبيوتر | الجوال |
| --- | --- | --- |
| مدة الدورة | **36s** | **45s** |
| مقاس الخانة | 216 × 109px | 156 × 82px |
| الفراغ بين الخانات | 86.4px | 42.9px |

الحركة `linear` و`infinite` من `translateX(0)` إلى `translateX(-50%)`.

### لماذا `margin-inline-end` وليس `gap`

سلاسة الدورة تعتمد على أن نصف عرض المسار يساوي **بالضبط** تسلسلًا واحدًا. مع
`gap` يكون عدد الفراغات أقل من عدد الخانات بواحد، فلا يقع `-50%` على نقطة
الوصل وتظهر قفزة. مع هامش خلفي على كل خانة تتساوى بصمة كل خانة، فتكون اثنتا
عشرة خانة ضعف ستٍّ تمامًا.

**التحقق:** عرض التسلسل 1814.34px = نصف عرض المسار بالضبط (فرق صفر)، ومواقع
الشعارات عند اللحظة 0 وعند نهاية الدورة متطابقة: 0, 302, 605, 907, 1210px.
الفرق البصري بين الإطارين 2.5٪ من البكسل وهو ناتج عن ترميز نصف بكسل لتحويل
كسري، لا عن إزاحة — الشعارات في المواضع نفسها والأحجام نفسها.

### اتجاه RTL

`direction: ltr` على **نافذة العرض** لا على المسار. النافذة هي التي تحدد أين
يُثبَّت طفلها الأعرض منها: تحت RTL كانت تثبّت الحافة **اليمنى** للمسار، فيظهر
عند اللحظة 0 التسلسل المكرر (`aria-hidden`) بينما التسلسل الحقيقي خارج الإطار
يسارًا. تثبيت النافذة LTR يبدأ الدورة على التسلسل الحقيقي.

### الوصول

- التسلسل الأول يحمل `aria-label="عملاء صَوْغ"` وشعاراته تحمل `alt` كاملًا.
- التسلسل المكرر `aria-hidden="true"` وكل صوره `alt=""` — يُنطق العملاء الستة
  مرة واحدة لا اثنتي عشرة.
- عند `prefers-reduced-motion` تتوقف الحركة تمامًا، ويخرج المكرر من التدفق،
  ويتحول التسلسل الحقيقي إلى شبكة ساكنة: ستة أعمدة على الكمبيوتر، ثلاثة على
  اللوحي، عمودان على الجوال.
- بلا روابط، بلا `cursor: pointer`، بلا نص مرئي، بلا عنصر قابل للتركيز.

### تعديلان في بوابات الفحص

1. **`release-qa.mjs`** كان يرصد أي عنصر يتجاوز حافة النافذة. الشريط يتجاوزها
   بالتصميم وتقصّه نافذته. صار الفحص يتجاهل التجاوز الواقع داخل سلف يقصّ
   أفقيًا — والتمرير الأفقي للصفحة (`horizontalOverflow`) يبقى مرصودًا كما هو.
2. **`arabic-typo-qa.mjs`** كان يرفض `alt` فارغًا. صار يتحقق مما يهم فعلًا:
   تسلسل معلن واحد بـ`aria-label` وبنصوص بديلة كاملة، وكل نسخة مكررة
   `aria-hidden` بـ`alt=""`، ولا شعار يتجاوز خانته.

التعديل الثاني يعوّض تغطية فقدها التعديل الأول: قصّ الشعار داخل خانته لم يعد
يظهر في فحص حواف الصفحة، فصار يُفحص مباشرة.

---

## v2.2 — تصحيح الطباعة العربية وشبكة العملاء

التاريخ: 2026-09-10
النطاق: تصحيح بصري داخل v2.1 — **بلا نشر**، وبلا تغيير في النصوص أو الوظائف
أو التحليلات أو إعدادات النشر.

### تزاحم العناوين العربية

هذا الإصدار **يعكس** قرار v2.1 بتضييق ارتفاع السطر. القياس الفعلي في المتصفح
قبل التعديل:

| | قبل | بعد |
| --- | --- | --- |
| ارتفاع سطر Hero | 1.08 | **1.26** |
| ارتفاع سطر عناوين الأقسام | 1.12 | **1.32** |
| تباعد الأحرف | −0.02em إلى −0.035em | **normal** |
| الفراغ بين الأسطر | 4–15.8px | **9–15px** |

`letter-spacing` السالب كان الخطأ الأكبر: العربية خط متصل، والتضييق يخنق
الوصلات ويزاحم النقاط. القيمة الآن `normal` في كل العناوين.

**ارتفاع السطر وحده لم يكن كافيًا.** بالقياس على البكسل المرسوم: عند 1.28 بلا
فراغ، تداخل ذيل «ق» في «نفهم قبل» مع «أن نصوغ.» تحته بمقدار 8.6px عند حجم
48px. إغلاق ذلك بارتفاع السطر وحده يحتاج ~1.50، خارج أي نطاق معقول — لأن حبر
Alexandria يتجاوز صندوق سطره عند أحجام العرض. لذلك بقي ارتفاع السطر عند سقف
النطاق المطلوب (1.22–1.32) وتكفّل `gap` بالفصل.

### حجم العنوان يتبع عرض عموده

أُضيف `container-type: inline-size` على أعمدة النص، وصار حجم العنوان بوحدة
`cqi` بدل `vw` وحدها. النتيجة على نسبة امتلاء العمود (أطول سطر ÷ عرض العمود):

| العرض | Hero | التواصل | الذكاء الاصطناعي |
| --- | --- | --- | --- |
| 1024px | 99.8٪ → **78٪** | 100٪ → **79.9٪** | 95٪ → **73.4٪** |
| 1440px | 86.6٪ → **77.4٪** | 90.6٪ → **74.8٪** | 100٪ → **72.8٪** |
| 390px | 90.1٪ → **81.4٪** | 75.1٪ → 80٪ | 100٪ (يلتف طبيعيًا) |

أثر جانبي مقصود: الحجم لم يعد رتيبًا عبر العروض. عند 768px يكون عمود Hero
بعرض الصفحة كاملة فيكبر العنوان (50.9 → 60px)، وعند 1024px يصبح العمود نصف
الشبكة فيصغر (59.8 → 46.2px). هذا هو السلوك الصحيح: العنوان يتبع عموده لا
النافذة. نسبة الامتلاء هي الحارس، وهي صحية عند كل عرض.

عُرّض عمود عنوان قسم الذكاء الاصطناعي من `62ch` إلى `76ch` لأن المشكلة كانت
ضيق العمود لا كِبَر الخط.

### مسافات المجموعة الواحدة (Hero عند 1440px)

| | قبل | بعد | المستهدف |
| --- | ---: | ---: | --- |
| العنوان الإنجليزي ← العربي | 20px | **26px** | 20–28 |
| العنوان ← الوصف | 22px | **30px** | 24–32 |
| الوصف ← الأزرار | 30px | **36px** | 28–36 |
| ارتفاع سطر الوصف | 1.72 | **1.75** | 1.65–1.8 |

### قص كان يبتر الحروف

`.hero` و`.intel` كانتا تحملان `overflow: clip` على المحورين لاحتواء الزخرفة
خارج الإطار — وكانتا تقصّان ذيول الحروف والتشكيل لأي عنوان قرب حافة القسم.
صارتا `overflow-x: clip`: يحتوي الزخرفة أفقيًا، ويترك الحبر الرأسي حرًا (بخلاف
`hidden`، لا يُجبر `clip` المحور الآخر على `auto`).

### شبكة العملاء

استُبدل الصف المتباعد بشبكة ثابتة: ثلاثة أعمدة وصفّان على الكمبيوتر، وعمودان
وثلاثة صفوف على الجوال، بعرض أقصى 1120px، وخلايا بارتفاع 172px (136px على
الجوال)، وخلفية `--surface-raised` موحدة، وخطوط فاصلة رفيعة بين الخلايا،
بلا ظلال ولا زخارف ولا تفاعل.

كل شعار له ميزانية عرض/ارتفاع مستقلة كنسبة من الخلية: الشعارات العريضة تأخذ
عرضًا وتتنازل عن ارتفاع، والطولية والدائرية بالعكس. `object-fit: contain`،
والنِّسب الأصلية لا تُمس.

**ملاحظة تقنية:** الميزانية الرأسية تُحسب بالبكسل من ارتفاع الخلية لا كنسبة
مئوية — داخل عنصر شبكة يتحدد ارتفاعه بمحتواه، لا تجد النسبة المئوية أساسًا
تُحلّ عليه، وكانت الشعارات العريضة تنكمش إلى خُمس ميزانيتها.

### الترويسة عند تكبير 200٪

عند 390px مع تكبير 200٪ كان زر القائمة يخرج 42px خارج الشاشة، لأن الشعار
والزر معًا أعرض من النافذة والشعار غير قابل للانكماش. أصبح
`flex: 0 1 auto` مع `max-width: 100%`.

### أدوات فحص جديدة

- `npm run qa:typography` — بوابة: ارتفاع السطر داخل النطاق، تباعد الأحرف،
  عدم وجود سلف يقص، ارتفاع سطر النصوص، احتواء الشعارات، غياب أي تفاعل في قسم
  العملاء، وغياب التمرير الأفقي — عبر 360/390/768/1024/1440.
- `npm run qa:ink` — يقيس البكسل المرسوم فعلًا: يلتقط كل عنوان بدقة 2× ويحسب
  صفوف الفراغ بين أسطر الحبر. **يبلّغ ولا يحكم**: الأسطر العربية تتداخل أفقيًا،
  فقد لا يفصل بينها صف فارغ واحد وهي مقروءة تمامًا. القرار بصري.
- `npm run qa:sections` — لقطات لكل قسم على حدة بدل صفحة طويلة.

---

## v2.1 — قرارات المالك: العملاء الستة، إزالة الفريق، ضبط الإيقاع

التاريخ: 2026-09-09
النطاق: تعديلات على v2.0 — **بلا نشر**

### قسم العملاء

- أصبح **ستة شعارات**: أُضيفت دار ورد وموتاس كافيه وبنيان الفخامة للتمليك من
  ملفات رسمية زوّدها المالك. لم يعد هناك شعار مفقود.
- ثلاثة منها تحمل خلفيتها كجزء من التصميم (ذهبي على كحلي، أبيض على أزرق فاتح،
  شارة زيتونية). في حالتين رفعُ الخلفية يمحو الشعار، فبقيت وعُلّمت بـ`plate`
  فتأخذ زاوية 6px. التفاصيل في `CLIENT-LOGOS.md`.
- الشريط صار صفًا واحدًا على الكمبيوتر، وثلاثة أعمدة على اللوحي، وعمودين على
  الجوال. أُعيد ضبط `opticalScale` لكل شعار على حدة.

### إزالة قسم الفريق

بقرار المالك: حُذف قسم الفريق بالكامل — لا أسماء ولا أدوار ولا بطاقات ولا
`team.ts` ولا أنماط، ولا كيانات `Person` في البيانات المنظمة (لم تكن موجودة
أصلًا). قسم «من نحن» بقي وحلّت محل الفريق ثلاث نقاط عن طريقة عمل الوكالة.

### ضبط الإيقاع الرأسي والطباعي

- `--section-y` من `clamp(72px, 8.5vw, 132px)` إلى `clamp(54px, 6.2vw, 96px)`.
- ارتفاع سطر العناوين العرضية: 1.12 للعام و1.08 للكبير — داخل النطاق المطلوب.
- **لكن** الضبط لم يُنفَّذ بارتفاع السطر وحده: عند 1.04 اصطدم ذيل «غ» في
  «نصوغ» بحرف السطر التالي. الفصل الحقيقي يأتي من `gap` بين الأسطر
  (10–18px للعنوان الكبير)، لأن كل سطر صندوق مستقل. هكذا بقيت العلامات
  التشكيلية والذيول ظاهرة بلا قص وبلا تصادم.
- تقليل المسافة بين العنوان والنص، وبين رأس القسم ومحتواه، وإحكام ارتفاع
  السطر في النصوص الثانوية.
- **الأثر:** ارتفاع الصفحة على 1440px من 7138px إلى 6310px (−12٪)، وعلى
  390px من 9943px إلى 9036px (−9٪).

### تحقق

`npm run qa:release` مرّت على بناء الإنتاج عبر تسع مقاسات، وفُحصت العناوين
بصريًا بالدقة الأصلية على 1440px و390px و360px للتأكد من عدم القص أو التصادم.

---

## v2.0 — إعادة تصميم وتطوير كاملة

التاريخ: 2026-09-09
النطاق: إعادة بناء الصفحة الرئيسية ونظام التصميم — **بلا نشر**

### التموضع والمحتوى

- رسالة الواجهة أصبحت «نصوغ الأفكار إلى نمو قابل للقياس.» مع نص داعم يربط
  الاستراتيجية والإبداع والتقنية بالأثر التجاري.
- قسم تموضع جديد «أكثر من وكالة تسويق.» يوضح المنظومة الواحدة في أربعة عناصر.
- الخدمات أُعيد تنظيمها في خمس مجموعات، كل مجموعة تبدأ بالنتيجة التجارية قبل
  أن تسرد ما بداخلها.
- المنهج أصبح أربع مراحل على مسار واحد متصل بدل أربع بطاقات متكررة.
- قسم ذكاء اصطناعي جديد: «ذكاء اصطناعي يعمل داخل المنظومة، لا فوقها» — ست
  استخدامات فعلية، وبلا ادعاء منصة مملوكة.
- قسم فريق بعضوين معتمدين، بمعالجة طباعية بدل صور مولدة.
- قسم «عملاؤنا»: ثلاثة شعارات رسمية، بلا اسم ولا وصف ولا رقم ولا رابط. انظر
  `CLIENT-LOGOS.md`.

### نظام التصميم

- `src/styles/tokens.css` أصبح المصدر الوحيد للألوان والمسافات والخطوط والحركة؛
  الألوان الست المعتمدة هي الأصل وكل ما عداها اشتقاق منها.
- سلّم طباعي مرن بالكامل بـ`clamp()`، وحاوية موحدة، ونظام خطوط شعرية بدل الظلال.
- `App.tsx` تفكّك من ملف واحد بـ503 أسطر إلى مكون لكل قسم، والمحتوى انتقل إلى
  `src/data/`.
- عنصر بصري أصلي (`ForgeField`): من معطيات متفرقة إلى مسار نمو واضح، بـSVG
  خالص، بلا مكتبة ولا صورة نقطية.

### إمكانية الوصول

- إصلاح اتجاه صناديق `.ltr` داخل صفحة RTL — كانت العناوين الفرعية تلتصق بالحافة
  اليسرى بدل اليمنى.
- `bdi` عاد إلى `dir="auto"` بدل الإجبار على LTR، وإلا انقلب اسم الكيان القانوني.
- رفع لون النص الثانوي إلى `#5E6470` ليحقق 4.5:1 على كل خلفيات النظام، ونقل
  بطاقة الخدمة الأولى من `mist` إلى `sand` لأن الكوبالت يهبط إلى 3.98:1 على mist.
- كل حقل بعلامة حقيقية، وكل خطأ برسالة نصية لا بلون فقط، وترتيب عناوين بلا قفزات.

### SEO

- الدومين الرسمي `https://soghagency.com` تم التحقق من أنه يعمل ويخدم الموقع،
  فأُزيل `noindex`، وأُضيف canonical وog:url وog:image مطلق وsitemap.
- بيانات منظمة: `Organization` + `ProfessionalService` + `WebSite` — بلا عنوان
  أو مدينة لأن العنوان الوطني غير موثق في المشروع.

### الأداء

- **استضافة الخطوط محليًا:** IBM Plex Sans Arabic وSpace Grotesk انتقلا من
  Google Fonts إلى `public/fonts/` — بمجموعتي المحارف العربية واللاتينية فقط
  (232 كيلوبايت في 8 ملفات). لم يعد هناك أي ملف أنماط خارجي على المسار الحرج،
  ولا أي اتصال بـ`googleapis` أو `gstatic`. هذا نفس النهج الذي كان متبعًا
  أصلًا مع Alexandria.
- تحميل مسبق للوجهين اللذين ترسم بهما الشاشة الأولى فقط: العربية العرضية
  والعربية النصية بوزن 400.
- شعارات العملاء: WebP بلا فقد بمقاس ثلاثة أضعاف أكبر ارتفاع معروض —
  من 518 كيلوبايت إلى 96 كيلوبايت.
- `width`/`height` على كل صورة لمنع انزياح التخطيط.
- بلا مكتبة حركة جديدة: CSS وIntersection Observer فقط.

الأثر على Lighthouse (بناء إنتاجي، محاكاة جوال):

| | قبل | بعد |
| --- | ---: | ---: |
| الأداء | 81 | **89** |
| أول رسم للمحتوى | 3.5 ث | **2.4 ث** |
| مؤشر السرعة | 4.8 ث | **2.4 ث** |
| إمكانية الوصول | 100 | 100 |
| أفضل الممارسات | 100 | 100 |
| SEO | 100 | 100 |

### أدوات الفحص

- `npm run qa:shots` أعيدت كتابته: مصفوفة العرض القابلة للضبط عبر `QA_WIDTHS`،
  والتقاط تحت `prefers-reduced-motion` افتراضيًا ليكون كل تشغيل مطابقًا للسابق.
- `npm run qa:release` أعيدت كتابته على الترميز الجديد، وأضيف إليه: بقاء زر
  الإجراء فوق الطية، وقواعد شعارات العملاء، وحالة الحركة المخفّضة، وصحة
  البيانات المنظمة.

---

## v1.0 r2 — Release Notes

Date: 2026-07-18  
Scope: final local production handoff; no deployment

## Fixed

- Removed the global `body` minimum width that caused `15px` horizontal
  overflow when a Windows classic scrollbar reduced a `320px` window to a
  `305px` document client width.
- Replaced the symptom-masking horizontal clip with a naturally fitting root
  layout (`html`, `body`, `#root`: `width: 100%`, `min-width: 0`,
  `max-width: 100%`).
- Added shrink-safe sizing to display headings and single-column responsive
  grids.
- Added responsive maximum sizing for image, SVG, video, and canvas media
  without forcing their width.
- Added narrowly scoped diagnostic font clamps below `340px` for Method and
  below `300px` for the AI title. Supported widths and desktop appearance are
  unchanged.

## QA tooling

- Added `npm run qa:release`, covering the complete responsive matrix,
  visible-edge detection, font and zoom state, Hero invariants, keyboard focus,
  mobile navigation, form validation/email preparation, accessibility
  structure, allowed links, metadata, and console errors.
- Updated the typography QA to treat the approved desktop Work heading as one
  visual line while retaining explicit semantic spans.

## Preserved exactly

Approved identity, Arabic copy, Alexandria typography, Hero composition,
Hero punctuation ratio, Hero/AI motion sequence, contacts, navigation, section
order, form behavior, and all other signal lines remain unchanged.

## Production output

- JavaScript: `assets/index-CsqcPn68.js`
- CSS: `assets/index-FJ0w4iAL.css`
- Status: TypeScript and Vite build passed; zero audit vulnerabilities.
- Deployment: not performed.
