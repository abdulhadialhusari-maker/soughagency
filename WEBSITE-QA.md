# فحص الموقع العام — Website QA

التاريخ: 2026-07-17 · النطاق: `http://localhost:5181` · البناء: `dist/`

## 1. البناء والكونسول

| البند | النتيجة |
| --- | --- |
| `tsc --noEmit && vite build` | ✓ نظيف (159KB JS / 17KB CSS قبل الضغط) |
| أخطاء الكونسول (فحص آلي على 4 مقاسات) | ✓ صفر |
| بناء نهائي بعد التعديلات | ✓ |

## 2. التجاوب وRTL

| البند | النتيجة |
| --- | --- |
| 1440 / 1280 / 768 / 375 | ✓ لقطات كاملة في `qa-previews/site-*.png` |
| تمرير أفقي | ✓ صفر بكسل على المقاسات الأربعة (فحص آلي scrollWidth) |
| RTL | ✓ `dir=rtl` + خصائص منطقية؛ تحقق بصري على اللقطات |
| قائمة الجوال | ✓ تفتح بـaria-expanded، تُغلق بالرابط وبـEscape، مخفية `display:none` عند الإغلاق (لا فخ تركيز) |

## 3. الوصولية

| البند | النتيجة |
| --- | --- |
| رابط التخطي | ✓ أول عنصر، يظهر عند التركيز |
| حالات التركيز المرئية | ✓ `:focus-visible` كوبالت 3px |
| تسميات النموذج | ✓ label لكل حقل + `aria-invalid` + `aria-describedby` لرسائل الخطأ |
| التركيز على أول حقل خاطئ عند الإرسال | ✓ (أُصلح: تركيز مباشر بدل rAF المعطل في التبويبات الخلفية) |
| `prefers-reduced-motion` | ✓ قاعدة عامة تصفّر الحركة؛ كل الحركات تعمل مرة واحدة (لا حلقات لا نهائية) |
| HTML دلالي | ✓ header/main/footer/nav/section + landmarks |

## 4. الروابط والنموذج

| البند | النتيجة |
| --- | --- |
| روابط التنقل الستة + CTAs | ✓ hash anchors تعمل |
| الهاتف | ✓ `tel:+966536774357` |
| البريد | ✓ `mailto:soghagency@gmail.com` |
| السوشيال | ✓ إنستغرام/تيك توك/سناب فقط — لا واتساب، لا X/لينكدإن (مخفية لأنها فارغة) |
| تحقق النموذج | ✓ 4 حقول مطلوبة برسائل عربية |
| رسالة mailto | ✓ عنوان «طلب مشروع جديد — …» + جسم منسق (اسم/شركة/هاتف/خدمة/نبذة) |
| استعادة الواجهة بعد التجهيز | ✓ رابط مراجعة + النموذج يبقى قابلًا للاستخدام |

## 5. المحتوى والثقة (فحص آلي بلا نتائج)

لا وجود لأي من: نضمن النتائج/المبيعات/الانتشار، الأفضل في السوق، آدم محمد،
sogh.sa أو أي دومين وهمي، واتساب، بيانات placeholder، مصطلحات داخلية
(الدليل/المختبر/التصدير/حقيبة العميل/مسودة)، شهادات أو أسماء عملاء أو
إحصاءات مخترعة. نماذج العمل موسومة صراحة كمسارات توضيحية مع بيان أن دراسات
الحالة تُنشر بعد موافقة العميل.

## 6. تغييرات هذه الجولة على المشروع القائم

1. نصوص: «لا نبدأ بالتصميم…» (بدل بالإجابة)، إضافة «الذكاء الاصطناعي» لنص
   التموضع، وإضافة «الإنتاج» و«المسؤولية» لقسم الذكاء — مطابقة للرسائل المعتمدة.
2. إصلاح تركيز أول حقل خاطئ (rAF → مباشر).
3. `npm run sync:brand` جديد + `scripts/sync-brand.mjs` (13 ملفًا من المستودع القانوني).
4. `npm run qa:shots` جديد لفحص التجاوب والكونسول آليًا.
5. README محدث بالكامل + هذا الملف.

## 7. فحص الإنتاج الحي — 2026-07-17 (نشر Vercel)

الرابط: **https://sogh-agency.vercel.app** · مشروع `sogh-agency` · التفاصيل في `DEPLOYMENT.md`

| البند | النتيجة |
| --- | --- |
| HTTP 200 + HTTPS (HSTS مفعل) | ✓ |
| أحدث بناء (تطابق hash الأصول مع dist المحلي) | ✓ `index-UjU5Z60m.js` / `index-DwS6v5_G.css` |
| ترويسات الأمان الأربع | ✓ من `vercel.json` |
| noindex مؤقت + robots.txt | ✓ يعملان — الزوار غير محجوبين |
| 14 أصل إنتاج (خطوط/شعارات/أيقونات/OG) | ✓ 200 جميعًا بأنواع MIME صحيحة |
| كونسول الإنتاج على 4 مقاسات | ✓ صفر أخطاء |
| تمرير أفقي على 4 مقاسات | ✓ صفر بكسل |
| الخطوط والشعار العربي | ✓ fonts: loaded، الشعار naturalWidth > 0 |
| قائمة الجوال + Escape على الإنتاج | ✓ |
| تحقق النموذج + التركيز على أول حقل خاطئ | ✓ على الإنتاج |
| رسالة mailto (المستلم والعنوان) | ✓ soghagency@gmail.com |
| الهاتف/السوشيال | ✓ tel:+966536774357 + إنستغرام/تيك توك/سناب فقط |
| لا واتساب / لا X/لينكدإن / لا دومين وهمي | ✓ فحص آلي على DOM الإنتاج |
| reduced-motion | ✓ القاعدة ضمن CSS الإنتاج (نفس hash النسخة المفحوصة محليًا) |
| لقطات الإنتاج | ✓ `qa-previews/prod-{1440,1280,768,375}.png` |

## 8. فحص الهوية المتحركة — 2026-07-17

**سبب غياب الحركة سابقًا (تشخيص):** لم تكن هناك حركة علامة أصلًا — الشعار كان
`<img>` ثابتًا لا يمكن رسمه، و«الإشارة» خط تدرج CSS عام تظهر نقطته من أول
إطار، وقسم الذكاء كان يتحرك **عند تحميل الصفحة** (وهو خارج الشاشة) فلا يراه
أحد، وكل المؤقتات كانت تبدأ مع أول paint وتنتهي خلال ~1.4 ثانية متداخلة مع
تحميل الخطوط. لا علاقة للأمر بإعداد تقليل الحركة (مؤكد أنه غير مفعل).

**ما أُضيف:** `src/motion.tsx` — علامات SVG inline بمسارات الهوية المعتمدة
(مرآة للمستودع القانوني) + خطافا `useMotionAllowed` و`useInViewOnce`؛ تسلسل
البطل: ضوضاء (0–380ms) → خط يصفو (330–890ms) → النقطة الكوبالتية (880ms) →
رسم صَوْغ حرفًا حرفًا (480–1550ms) → التشكيل (1300–1740ms) مع استقرار النص
(250–1130ms) وخط «للنمو» (1150ms) — مرة واحدة ثم ثبات كامل؛ وقسم الذكاء
يتحرك مرة واحدة عند دخوله الشاشة (~1.1s) عبر IntersectionObserver.

| البند | النتيجة |
| --- | --- |
| الحالة الأساسية = الحالة النهائية (بلا JS/بتقليل الحركة: صفحة كاملة ثابتة) | ✓ |
| تقليل الحركة (محاكاة): dashoffset=0 والنقطة ظاهرة فورًا، بلا فئات حركة | ✓ فحص برمجي + لقطتا `reduced-*.png` |
| تعمل مرة واحدة؛ العودة بالتمرير لا تعيد التشغيل | ✓ فحص WAAPI آلي |
| `is-inview` يثبت وصول مراقب التقاطع في مستند مرئي | ✓ waitForSelector في كل تشغيل |
| لا انزياح تخطيط (أماكن العناصر ثابتة قبل/أثناء/بعد) | ✓ مقارنة الإطارات |
| كونسول نظيف + صفر تمرير أفقي على 375/560/768/1280/1440 | ✓ |
| لا تسميات داخلية (Route C / ident / إعادة الحركة) | ✓ |
| CTAs متاحة مبكرًا (استقرار الأزرار يكتمل ~1.1s، لا شاشة افتتاحية حاجبة) | ✓ |

**ملاحظة بيئة الفحص:** تبويب لوحة المعاينة مستند `hidden` — مراقب التقاطع
لا يعمل فيه إطلاقًا بحكم المواصفة؛ التحقق الصحيح يتم عبر Chrome headless
(مستند مرئي) أو متصفح عادي.

**التسجيلات والإطارات:** `qa-previews/motion/` — `hero-motion.webm`
(تسجيل الحركة الكاملة: تحميل + تمرير لقسم الذكاء)،
`hero-{before,during,after}.png`، `ai-{before,during,after}.png`،
`reduced-{hero,ai}.png`، `final-{375,560,768,1280,1440}.png`.

**اعتُمدت الحركة من المالك ونُشرت — انظر القسم 9.**

## 9. فحص نشر حركة الهوية — 2026-07-17 (النشرة `dpl_8eLeUkJpyj8iPfYBXfFQsQx19bM3`)

قبل النشر (على بناء الإنتاج محليًا عبر `npm run preview`) وبعده (على الرابط الحي) — النتائج متطابقة:

| البند | النتيجة |
| --- | --- |
| مزامنة العلامة | ✓ 13/13 |
| بناء الإنتاج + noindex/robots المؤقتان | ✓ (الدومين غير مسجل) |
| النسخة الحية = أحدث بناء | ✓ تطابق hash: `index-wMZZPdkI.js` / `index-XHgYyEdl.css` |
| حركة الهيرو الحية (إشارة → خط → نقطة → رسم الشعار → استقرار النص) | ✓ إطارات before/during/after |
| حركة قسم الذكاء عند التمرير حيًا | ✓ تكتمل باستقرار كامل (عنوان ونص وإشارة ونقطة) |
| التشغيل مرة واحدة (إعادة التمرير لا تعيد الحركة) | ✓ فحص آلي |
| reduced-motion محاكاة على الحي | ✓ حالة نهائية فورية: بلا صنف الحركة، `stroke-dashoffset: 0`، النقطة ظاهرة |
| 375 / 560 / 768 / 1280 / 1440 حيًا | ✓ صفر تمرير أفقي؛ تحقق بصري لـ560 (القائمة والطباعة العربية سليمتان) |
| كونسول الإنتاج | ✓ صفر أخطاء |
| النموذج حيًا | ✓ 4 أخطاء تحقق + تركيز أول حقل + mailto إلى soghagency@gmail.com |
| الروابط حيًا | ✓ tel:+966536774357 + إنستغرام/تيك توك/سناب فقط — لا واتساب/X/لينكدإن/دومين وهمي |
| تسجيل الحركة الحي | ✓ `qa-previews/motion-live/hero-motion.webm` + إطارات المراحل في `qa-previews/motion-live/` |
| التراجع | النشرة السابقة `dpl_7Y1xdijdHVKHaXP9UhAX9BAYhvK8` (`jo17druue`) ما تزال Ready — `npx vercel rollback` |

## 10. المتبقي للإطلاق الرسمي

1. تسجيل الدومين ثم تنفيذ قائمة إطلاق الدومين في `DEPLOYMENT.md` (canonical/og:url/sitemap + إزالة noindex).
2. لاحقًا: استبدال mailto بباك-إند نموذج مُراجع الخصوصية.

## 11. إصلاح تباعد العناوين العربية V2 — 2026-07-18 (محلي فقط، بلا نشر)

أُعيدت هذه الجولة بعد رفض الإصلاح الأول بصريًا. استُخدم تسجيل المستخدم عند
`1338×628` كمرجع Before، ولم يُعتمد نجاح DOM وحده بديلًا عن الفحص البصري.

### التشخيص المصحح

- المشروع لا يحتوي `.git`، لذلك لم يكن `git status` أو `git diff` متاحًا.
- لم توجد تعديلات غير مكتملة في `motion.tsx`؛ الموشن المعتمد بقي كما هو.
- كان العنوان كتلة واحدة تلتف تلقائيًا. عند 1338px كان الحجم `62.217px`
  والتتبع `-2.80px` مع `text-wrap: balance`. كانت صناديق الأسطر غير متداخلة،
  لكن حبر Alexandria العربي ونقاطه وتشكيله ظل متقاربًا بصريًا؛ وهذا سبب فشل
  الإصلاح الأول رغم اجتياز القياسات التقليدية.

### القواعد النهائية

1. صارت العناوين الأربعة المستهدفة سطرين صريحين داخل
   `.display-title__line`، مع `aria-label` يحفظ الاسم الكامل لقارئ الشاشة.
2. تستخدم `.display-title` flex عموديًا وفراغًا فيزيائيًا مستقلًا بين السطرين:
   `14px` عند 320–375، و`18px` في العروض المتوسطة، و`19.39px` عند 1338.
3. الحجم `clamp(2rem, 4.05vw, 3.75rem)`؛ عند 1338 أصبح `54.189px`، أقل بنحو
   13% من الحالة المرفوضة، مع `line-height: 1.12` و`letter-spacing: normal`.
4. أزيل `text-wrap: balance` عن هذه العناوين، وبقي `overflow: visible`، بلا
   margin أو transform سالب.
5. بقيت عناوين البطاقات بتغليف تلقائي مع clamp آمن، تتبع طبيعي،
   `line-height: 1.50` (`1.52` على الشاشات الصغيرة)، وهوامش أوضح قبل الوصف.
6. لم تتغير النصوص أو الخط أو الشعار أو الألوان أو ترتيب الأقسام أو الموشن
   أو بيانات التواصل.

### نتائج QA النهائية

| الفحص | النتيجة |
| --- | --- |
| المقاسات الدقيقة 320×700، 375×812، 430×932، 480×900، 560×800، 640×900، 768×900، 900×900، 1024×768، 1280×800، 1338×628، 1440×900 | ✓ |
| الفحص البصري الكامل عند 375/560/768/1280/1338/1440 | ✓ لا تلامس ولا قص للحروف أو النقاط أو التشكيل |
| تمرير أفقي | ✓ صفر بكسل في جميع المقاسات |
| الفراغ الفيزيائي بين السطرين | ✓ من 14px إلى 20.88px حسب العرض |
| Alexandria / layout shift | ✓ الخط محمّل؛ CLS=0 عدا 0.00012 عند 900px |
| هندسة عنوان الذكاء قبل/أثناء/بعد الموشن | ✓ الارتفاع 140.766px والفراغ 19.39px ثابتان عند 1338 |
| الحالة النهائية للموشن | ✓ `opacity: 1` و`transform: none`؛ لا إعادة عند التمرير |
| `prefers-reduced-motion` | ✓ هندسة مطابقة وحالة نهائية فورية |
| Console | ✓ صفر أخطاء |
| `npm run build` | ✓ TypeScript + Vite؛ 163.19kB JS / 21.62kB CSS قبل الضغط |
| النشر | لم يُنفذ، حسب الطلب |

### اللقطات والبيانات

- Before من تسجيل المستخدم: `qa-previews/typography-v2/before/`.
- After كاملة الدقة: `qa-previews/typography-v2/after/after-*-1338x628.png`.
- Full-page: `qa-previews/typography-v2/after/full-{375x812,560x800,768x900,1280x800,1338x628,1440x900}.png`.
- القياسات الخام: `qa-previews/typography-v2/after/typography-metrics.json`.
- الموشن وتقليل الحركة: `qa-previews/motion/`.

التقرير المستقل: `TYPOGRAPHY-REPORT.md`.

## 12. إغلاق حاجز Windows Chrome وإصدار r2 — 2026-07-18

### السبب الجذري والإصلاح

- كان `body { min-width: 320px; overflow-x: clip; }` يبقي المستند بعرض
  `320px` بينما يخفض شريط تمرير Windows الكلاسيكي
  `document.documentElement.clientWidth` إلى `305px`. انتشر العرض القسري إلى
  `#root` والرأس والأقسام والتذييل؛ كان الإخفاء يحجب العرض الزائد ولا يصلحه.
- أصبحت `html, body, #root` تستخدم `width: 100%`, `min-width: 0`,
  `max-width: 100%` بلا إخفاء أفقي.
- ثُبّت `box-sizing: border-box` على العناصر والعناصر الوهمية، وأضيفت قيود
  `max-width: 100%` الآمنة إلى `img`, `svg`, `video`, `canvas` من دون فرض
  `width: 100%`.
- أضيف `min-width: 0` إلى نظام عناوين العرض، وتحولت الشبكات أحادية العمود إلى
  `minmax(0, 1fr)`. استُخدم clamp صغير لقسم Method تحت `340px` ولعنوان AI
  التشخيصي تحت `300px` فقط؛ المظهر المكتبي المعتمد لم يتغير.

### القياس الحاسم عند Windows Chrome 320×700

| القياس | النتيجة |
| --- | --- |
| `window.innerWidth` | `320px` |
| `documentElement.clientWidth` | `305px` |
| `documentElement.scrollWidth` | `305px` |
| التمرير الأفقي | `0px` |
| عنوان Method | left `15px`, right `290px`, width `275px` |
| سطر Method الثاني | left `28.125px`, right `290px`, width `261.875px` |
| Footer | left `0px`, right `305px`, width `305px` |
| العناصر المتجاوزة للحواف | صفر |

### بوابة الإصدار

| الفحص | النتيجة |
| --- | --- |
| الإنتاج عند 280/300/305/320/360/375/390/430/560/768/1024/1280/1338/1440 | ✓ `scrollWidth === clientWidth`، صفر عناصر متجاوزة |
| Method / Footer / Form / Header | ✓ داخل الحدود في كل المقاسات |
| Hero | ✓ لا خط سفلي، النقطة `60%`، لا تراجع بصري |
| الموشن العادي / مرة واحدة / AI عند الدخول | ✓ |
| `prefers-reduced-motion` | ✓ الحالة النهائية فورًا وبالهندسة نفسها |
| القائمة المحمولة | ✓ فتح، Escape، إغلاق بعد الانتقال |
| النموذج | ✓ 4 حقول مطلوبة، رسائل عربية، البيانات محفوظة قبل mailto، كل الحقول داخل الرسالة |
| الوصول | ✓ `lang=ar`, `dir=rtl`، تسلسل عناوين سليم، أسماء وصول، labels، skip link وfocus ظاهر |
| التباين | ✓ جميع الأزواج العامة WCAG AA للنص العادي؛ أقل نسبة `4.50:1` |
| الروابط | ✓ هاتف وبريد وإنستغرام وتيك توك وسناب فقط؛ لا واتساب/X/لينكدإن/نطاق وهمي |
| الفهرسة المؤقتة | ✓ `noindex, nofollow`، لا canonical ولا `og:url` وهمي |
| Console / صور مكسورة | ✓ صفر |
| `npm audit` | ✓ صفر ثغرات |
| `npm run build` | ✓ TypeScript + Vite؛ 30 وحدة |
| النشر | لم يُنفذ |

الأدلة النهائية موجودة في `qa-previews/release-r2/` و
`qa-previews/release-r2-motion/`. تقرير البوابة الآلي:
`qa-previews/release-r2/release-qa-report.json`.
