# شعارات العملاء — حالة الأصول

آخر تحديث: 2026-09-09

قسم «عملاؤنا» يعرض الشعارات فقط: بلا أسماء مرئية، بلا وصف، بلا أرقام، بلا
روابط، وبلا أي إيحاء بأن الشعار قابل للنقر. القاعدة مفروضة في الكود
(`src/data/clients.ts` + `src/components/Clients.tsx`) ويتحقق منها
`npm run qa:release` في كل إصدار — أي عنصر قيمته `src` تساوي `null` لا يُعرض.

**لا Placeholder ولا شعار مُعاد رسمه ولا بديل نصي.**

## الستة المعروضة

| العميل | الملف | المصدر | الأبعاد |
| --- | --- | --- | --- |
| عبيد عابد | `obaid-abid.webp` | `oabid.com.sa/images/logo.png` — مطابق بالبايت للنسخة داخل المشروع | 626×132 |
| أكاديفاي | `acadify.webp` | القفل الرسمي بخلفية فاتحة من أصول المشروع، ويطابق القفل على `acadifysa.com` | 226×244 |
| دار ورد | `dar-ward.webp` | ملف رسمي زوّده المالك (2026-09-09) | 459×460 |
| بنت البلد | `bint-albalad.webp` | شعار المتجر الرسمي من `cdn.salla.sa` | 446×111 |
| موتاس كافيه | `mutas-cafe.webp` | ملف رسمي زوّده المالك (2026-09-09) | 420×310 |
| بنيان الفخامة للتمليك | `binyan-alfakhama.webp` | ملف رسمي زوّده المالك (2026-09-09) | 333×390 |

جميعها في `public/clients/` بصيغة WebP.

## الشعارات ذات الخلفية المدمجة

ثلاثة من الشعارات تحمل خلفيتها كجزء من التصميم نفسه:

- **موتاس كافيه** — خط أبيض على أزرق فاتح.
- **بنيان الفخامة** — ذهبي على كحلي.
- **دار ورد** — شارة دائرية بلون زيتوني.

في حالتَي موتاس وبنيان، **رفع الخلفية يمحو الشعار** لأن الحبر أفتح منها. لذلك
بقيت الخلفية كما هي، وعُلّمت في البيانات بـ`plate: true` فتأخذ في CSS زاوية
خفيفة (6px) لتقرأ كأصل مصمَّم لا كقصّ خاطئ. أما دار ورد فهي شارة على ورقة
بيضاء، فأُزيلت الورقة فقط وبقيت الشارة بحوافها الشفافة.

### ما جرى على كل ملف

لا إعادة رسم ولا إعادة تلوين ولا تغيير في النسب:

1. **قص** إلى حدود التصميم. وللشعارات ذات الخلفية المدمجة، أُعيد بعدها هامش
   متساوٍ بلون الخلفية نفسها (16٪) — لأن القص الملاصق للحبر يبدو كخطأ.
2. **رفع الخلفية إلى شفافية** — للشعارات التي تسمح بذلك فقط، بملء متصل من
   الحواف يمسح الكتل الكبيرة من الأبيض المحايد دون أن يمس عنصرًا ملوّنًا فاتحًا
   (مثل الكريمي `#F5F0EA` في علامة أكاديفاي).
3. **تصغير LANCZOS** ثم حفظ WebP.

## ملاحظة تحتاج تأكيدك

الشعار المزوَّد لموتاس كافيه مكتوب بخط اليد ويُقرأ **«Mufa's»**، بينما ملفات
المشروع السابقة وقائمة العملاء تسمّيه **Mutas / موتاس**. أبقيتُ النص البديل
`alt="موتاس كافيه"` مطابقًا لقائمتك. إن كان الاسم التجاري الصحيح «موفاز» أو
«Mufa's»، فالتصحيح سطر واحد في `src/data/clients.ts`.

## إضافة أو استبدال شعار

1. ضع الملف في `public/clients/`.
2. في `src/data/clients.ts` املأ `src` و`width` و`height`، وأضف `plate: true`
   إن كانت الخلفية جزءًا من التصميم.
3. اضبط `opticalScale` حتى يتساوى الوزن البصري مع البقية — القيمة تضرب في
   ارتفاع الأساس، فالشارة الدائرية تحتاج قيمة أعلى من الشعار الأفقي.
4. `npm run build` ثم `npm run qa:release`.

لا حاجة لأي تعديل في `Clients.tsx` أو في CSS.
